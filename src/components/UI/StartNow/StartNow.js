import React from "react";
import classes from './StartNow.css';

const startNow = (props) => {
    return (
        <div className={classes.Clickable}>
            Start Now!
        </div>
    )
};

export default startNow;